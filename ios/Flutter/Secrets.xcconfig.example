// Copy to ios/Flutter/Secrets.xcconfig (gitignored) and fill in the key.
// Without it MAPS_API_KEY resolves empty, GMSServices is never given a key
// (see AppDelegate.swift), and every map view comes up blank.
// Android reads the same key from android/local.properties.
MAPS_API_KEY = CHANGEME
